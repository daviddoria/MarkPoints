cmake_minimum_required(VERSION 2.6)

PROJECT(MarkPoints)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE})

ADD_EXECUTABLE(MarkPoints MarkPoints.cxx)
TARGET_LINK_LIBRARIES(MarkPoints ${ITK_LIBRARIES})

